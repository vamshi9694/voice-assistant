# --- Control plane ---
fastapi>=0.115
uvicorn[standard]>=0.30
sqlmodel>=0.0.22
httpx>=0.27

# --- Media plane (agent) ---
# Local stack (M5 Pro, $0/min):
#   pip install "pipecat-ai[webrtc,silero,whisper-mlx,kokoro,local-smart-turn-v3,runner]"
# Hosted stack (premium tier):
#   pip install "pipecat-ai[webrtc,silero,deepgram,openai,cartesia,local-smart-turn-v3,runner]"
pipecat-ai>=1.5.0
loguru>=0.7

# --- Phone + SMS (Phase 1) ---
twilio>=9.0
